export async function loginUser(email: string, password: string) {
  return { message: "Logic đăng nhập chưa triển khai" };
}

export async function registerUser(
  name: string,
  email: string,
  password: string
) {
  return { message: "Logic đăng ký chưa triển khai" };
}
